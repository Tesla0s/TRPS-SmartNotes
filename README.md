# SmartNotes

**SmartNotes** — это высокопроизводительное десктопное приложение для управления знаниями, построенное на базе Python и Flet. Проект объединяет классический подход к ведению заметок (Markdown, локальное хранение) с передовыми возможностями LLM (Large Language Models) для автоматизации рутины: категоризации, рефакторинга текста и генерации контента.

Архитектура приложения спроектирована по принципу **Local-First**: все данные хранятся локально в SQLite, а обращение к облачным API происходит только по явному запросу пользователя для задач ИИ.

---

## 🚀 Ключевые возможности

### 📝 Редактор и Markdown
*   **Split-View интерфейс:** Одновременное редактирование и предпросмотр отрендеренного Markdown.
*   **Wiki-Links:** Поддержка связывания заметок через синтаксис `[[Название заметки]]` для создания персональной базы знаний (Zettelkasten).
*   **Интерактивные задачи:** Поддержка чек-листов (`- [ ]`), состояние которых синхронизируется с базой данных при клике в режиме просмотра.

### 🧠 AI-Integration (LLM)
Приложение интегрировано с агрегатором OpenRouter, что позволяет переключаться между моделями (Grok, Gemini, Meituan LongCat) без изменения кода.
*   **Авто-тегирование:** Анализ контекста заметки и генерация релевантных JSON-тегов.
*   **Умная редактура:** Режимы "Исправить грамматику", "Упростить", "Деловой стиль".
*   **Генерация метаданных:** Автоматическое создание ёмких заголовков и кратких выжимок (Summary).

### 🔍 Поиск и Навигация
*   **Full-Text Search (FTS5):** Мгновенный полнотекстовый поиск по базе данных с использованием виртуальных таблиц SQLite. Поддерживается поиск по префиксам и фразам.
*   **Фасетная фильтрация:** Комбинированный поиск по папкам, текстовому запросу и множественным тегам.

### 📂 Управление данными
*   **Иерархия:** Двухуровневая система организации (Папки -> Заметки).
*   **Экспорт:** Поддержка выгрузки документов в форматы `.md`, `.txt` и `.pdf` (с рендерингом CSS-стилей).

---

## 🏗 Архитектура и Структура проекта

Проект следует принципам **Separation of Concerns (SoC)**. Логика разделена на слои данных, бизнес-логики, внешних сервисов и пользовательского интерфейса.

### Корневая директория
*   **`main.py`**: Точка входа (Entry Point). Инициализирует логирование и запускает цикл событий Flet.
*   **`requirements.txt`**: Декларация внешних зависимостей.

### Модуль `app/`
Ядро приложения.

#### 1. Конфигурация и Данные
*   **`config.py`**: Централизованное управление константами и конфигурацией AI-моделей. Здесь определяются `PRIMARY_MODEL` (основная) и `FALLBACK_MODELS` (цепочка запасных моделей для отказоустойчивости).
*   **`database.py`**: Слой доступа к данным (DAO).
    *   Управляет схемой SQLite, миграциями и индексами.
    *   Реализует триггеры для автоматической синхронизации основной таблицы `notes` с поисковым индексом `notes_fts` (FTS5).

#### 2. Сервисы (`app/services/`)
Изолированные модули для взаимодействия с внешним миром или сложной обработки данных.
*   **`ai_client.py`**: Клиент для OpenRouter API. Реализует паттерн **Retry with Backoff** (повторные попытки с экспоненциальной задержкой) для обработки ошибок `429 Rate Limit`. Абстрагирует работу с промптами (system/user roles).
*   **`exporter.py`**: Логика конвертации Markdown в статические форматы (PDF, Plain Text).

#### 3. Пользовательский интерфейс (`app/ui/`)
Реализация View-слоя на базе Flet (Flutter wrapper).
*   **`app.py`**: Главный контроллер (State Management). Связывает UI-компоненты, базу данных и сервисы. Управляет состоянием приложения (выбранная заметка, активные фильтры).
*   **`layout.py`**: Логика кастомного **Splitter Widget**. Реализует алгоритм изменения размеров панелей с сохранением пропорций (Flex) и персистентностью настроек.
*   **`dialogs.py`**: Фабрика модальных окон. Выносит код создания диалогов (создание папки, настройки, подтверждение удаления) из основного контроллера для чистоты кода.

#### 4. Утилиты (`app/utils/`)
*   **`common.py`**: Вспомогательные инструменты, например, класс `Debouncer` для оптимизации автосохранения (снижает нагрузку на I/O при быстром вводе текста).

---

## 🛠 Технологический стек

| Компонент | Технология | Описание |
| :--- | :--- | :--- |
| **Language** | Python 3.10+ | Основной язык разработки. |
| **UI Framework** | Flet | Реактивный UI на базе Flutter. |
| **Database** | SQLite 3 | Локальная реляционная БД + FTS5 расширение. |
| **AI Provider** | OpenRouter API | Унифицированный доступ к LLM (Grok, Gemini, GPT, etc.). |
| **PDF Engine** | markdown-pdf | Рендеринг Markdown в PDF с поддержкой CSS. |

---

## ⚙️ Конфигурация AI

Приложение спроектировано как **Model-Agnostic**. Выбор моделей осуществляется в файле `app/config.py`. Текущая конфигурация нацелена на использование высокоскоростных бесплатных моделей для минимизации задержек интерфейса:

*   **Primary:** `meituan/longcat-flash-chat:free` (Высокий контекст, скорость).
*   **Fallback:** Цепочка из `x-ai/grok`, `google/gemini` и `openai/gpt-oss` обеспечивает доступность сервиса даже при нестабильности одного из провайдеров.